server:
  port: 8081

spring:
  application:
    name: display

product:
  ribbon:
    listOfServers: localhost:8082

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://127.0.0.1:8080/eureka
